# Mirror frozen-rail source fixture. Current simulation stops at the terminal unsupported airborne mixed rail/ball contact rather than claiming the top-left bank.
table brunswick_gc4_9ft
ball cue frozen left (5.67)
ball six frozen left (5.85)
cue_strike(default).mass_ratio(1.0).energy_loss(0.1)
trace(max_events: 12)
shot(cue).heading(348deg).speed(88ips).tip(side: -0.22R, height: 0.28R).using(default)
