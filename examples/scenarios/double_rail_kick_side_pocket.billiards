# Double-rail kick: cue goes right rail then top rail into the object, which later drops in the right side pocket.
ball cue at center
ball one at (0.8, 5.0)
cue_strike(default).mass_ratio(1.0).energy_loss(0.1)
shot(cue).heading(30deg).speed(128ips).tip(side: 0.0R, height: 0.3R).using(default)
